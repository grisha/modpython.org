*** C:\Python25\Lib\site-packages\mod_python\psp.py	Thu Apr 26 09:30:15 2007
--- C:\Python25\Lib\site-packages\mod_python\psp_orig.py	Thu Apr 26 09:31:56 2007
***************
*** 197,204 ****
  
      def run(self, vars={}, flush=0):
  
-         class AbortException(Exception): pass
- 
          code, req = self.code, self.req
  
          # does this code use session?
--- 197,202 ----
***************
*** 227,233 ****
  
          try:
              global_scope = globals().copy()
!             global_scope.update({"req":req, "form":form, "psp":psp, "AbortException": AbortException})
  
              # strictly speaking, session attribute only needs
              # to be populated if referenced, but historically
--- 225,231 ----
  
          try:
              global_scope = globals().copy()
!             global_scope.update({"req":req, "form":form, "psp":psp})
  
              # strictly speaking, session attribute only needs
              # to be populated if referenced, but historically
***************
*** 250,258 ****
                  # (access time), so it *always* has to be saved
                  if hasattr(req, 'session'):
                      req.session.save()
-             except AbortException:
-                 pass
-             
              except:
                  et, ev, etb = sys.exc_info()
                  if psp.error_page:
--- 248,253 ----