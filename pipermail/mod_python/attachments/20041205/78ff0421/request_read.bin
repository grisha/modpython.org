--- src/requestobject.c.orig	2004-12-05 11:28:06.000000000 +0100
+++ src/requestobject.c	2004-12-05 12:45:58.950648992 +0100
@@ -465,9 +465,9 @@
 
     /* read it in */
     Py_BEGIN_ALLOW_THREADS
-    chunk_len = ap_get_client_block(self->request_rec, buffer, len);
+    chunk_len = ap_get_client_block(self->request_rec, buffer+copied, len);
     Py_END_ALLOW_THREADS
-    bytes_read = chunk_len;
+    bytes_read = chunk_len+copied;
 
     /* if this is a "short read", try reading more */
     while ((bytes_read < len) && (chunk_len != 0)) {
